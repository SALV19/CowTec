<!-- KPI Dashboard Section Organism -->
<div class="kpi-grid<%= typeof additionalClasses !== 'undefined' && additionalClasses ? ' ' + additionalClasses : '' %>">
  <% kpis.forEach((kpi, index) => { %>
    <div class="kpi-cell">
      <%- include('../molecules/kpi-card', {
        title: kpi.title,
        value: kpi.value,
        prefix: kpi.prefix,
        suffix: kpi.suffix,
        icon: kpi.icon,
        bgColor: kpi.bgColor,
        id: kpi.id,
        valueId: kpi.valueId,
        change: kpi.change,
        changeType: kpi.changeType
      }) %>
    </div>
  <% }); %>
</div>